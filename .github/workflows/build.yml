name: Build

on:
  push:
    branches:
      - main
      - cibuild

jobs:
  build:
    runs-on: macos-13-xlarge
    steps:
      - run: |
            xcode-select -p
            uname -a
            find /opt/homebrew -name omp.h
            mdfind -name omp.h
